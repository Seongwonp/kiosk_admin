<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cafe_kiosk.kiosk_admin.mapper.AdminUserMapper">
    <insert id="insert" parameterType="com.cafe_kiosk.kiosk_admin.dto.user.Admin">
        INSERT INTO admin (admin_id, admin_pw, admin_role, created_at, modified_at)
        VALUES (#{adminId}, #{adminPw}, #{adminRole}, NOW(), NOW())
    </insert>
    <select id="findByAdminId" parameterType="String" resultType="com.cafe_kiosk.kiosk_admin.dto.user.Admin">
        SELECT * FROM admin WHERE admin_id = #{adminId}
    </select>
    <select id="adminList" parameterType="map" resultType="com.cafe_kiosk.kiosk_admin.dto.user.Admin">
        SELECT admin_id, admin_role, created_at, modified_at FROM admin
        <where>
            <if test="op == 'id'">
                admin_id LIKE CONCAT('%', #{keyword}, '%')
            </if>
            <if test="op == 'name'">
                admin_name LIKE CONCAT('%', #{keyword}, '%')
            </if>
            <if test="op == 'role'">
                admin_role = #{keyword}
            </if>
        </where>
    </select>
    <select id="adminRoleCount" resultType="com.cafe_kiosk.kiosk_admin.dto.user.AdminRoleStats">
        SELECT admin_role AS adminRole, COUNT(*) AS cnt
        FROM admin
        GROUP BY admin_role
    </select>

    <update id="modifySelfPassword" parameterType="com.cafe_kiosk.kiosk_admin.dto.user.Admin">
        UPDATE admin
        SET admin_pw = #{adminPw},
            modified_at = NOW()
        WHERE admin_id = #{adminId}
    </update>

    <update id="modifyByCEO" parameterType="com.cafe_kiosk.kiosk_admin.dto.user.Admin">
        UPDATE admin
        SET admin_pw = #{adminPw},
            admin_role = #{adminRole},
            modified_at = NOW()
        WHERE admin_id = #{adminId}
    </update>
</mapper>